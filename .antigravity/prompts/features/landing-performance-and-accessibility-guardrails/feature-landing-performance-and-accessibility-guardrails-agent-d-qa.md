# Agent D (QA) — landing-performance-and-accessibility-guardrails

Checklist:

- `npm run -s lint`
- `npm run -s test`
- `npm run -s build`
- Lighthouse mobile + desktop
- Validación visual en breakpoints críticos

Emitir: GO / NO-GO con evidencia.
