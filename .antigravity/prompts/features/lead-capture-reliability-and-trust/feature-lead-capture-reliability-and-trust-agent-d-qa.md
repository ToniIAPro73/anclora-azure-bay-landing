# Agent D (QA) — lead-capture-reliability-and-trust

Checklist:

- Casos felices y de error del formulario.
- Validación visual + accesibilidad de inputs/mensajes.
- `npm run -s lint`, `npm run -s test`, `npm run -s build`.
- Lighthouse si cambia ruta crítica.
