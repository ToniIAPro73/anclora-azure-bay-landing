# landing-performance-and-accessibility-guardrails-test-plan-v1

1. Ejecutar `npm run -s lint`.
2. Ejecutar `npm run -s test`.
3. Ejecutar `npm run -s build`.
4. Lighthouse desktop y mobile en modo limpio (sin extensiones).
5. Verificación visual en 390px, 412px, 768px, 1280px.
6. Validación a11y: contrast, labels, heading order.
